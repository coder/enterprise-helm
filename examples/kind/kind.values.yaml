# Values for a small local test deployment using Kind
ingress:
  useDefault: false

coderd:
  resources:
    requests:
      cpu: 100m
      memory: 64Mi

timescale:
  resources:
    requests:
      cpu: 100m
      memory: 64Mi
